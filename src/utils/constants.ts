/**
 * Endpoints del backend interno
 */
const BackendApiEndpoints = {
  instruments: {
    getAll: {
      resource: "/api/instruments",
      method: "GET",
    },
  },
};

export { BackendApiEndpoints };
